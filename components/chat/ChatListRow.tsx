import React from 'react'
import { Skeleton } from '../ui/skeleton'

interface ChatListRowProps{
  chatId: string
}

export default function ChatListRow({chatId} : ChatListRowProps) {
  return (
    <div>ChatListRow</div>
  )
}
